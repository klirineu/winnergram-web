export const MainTheme = {
  primary: "#2F5AA3",
  primaryHover: "#1099FA",
  input: "#F6F6F6",
  placeholder: "#2B2A2A",
  gradient: "linear-gradient(to bottom, #2F5AA2, #14438B)",
  fadeTransition: "@keyframes fade {from{opacity: 0;} to{opacity: 1;}}",
  scaleTransition:
    "@keyframes scale {from{transform: scale(0.85);} to{transform: scale(1);}}",
};
